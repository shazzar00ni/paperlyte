/**
 * Test setup file for Vitest
 *
 * This file is run before all test files and can be used to:
 * - Set up global test configuration
 * - Configure mocks and test utilities
 * - Set up DOM environment
 */

// Example: Global test utilities could be added here
// global.testHelpers = { ... };

// Example: Configure global mocks
// vi.mock('some-module', () => ({ ... }));

// Example: Set up DOM environment if needed
// import 'jsdom-global/register';

console.log('Test environment initialized')
